# Pinned loosely, matching pyproject. The Space installs these; the package
# itself is copied in by sync.sh rather than installed, so a change to the
# engine needs no version bump here.
gradio>=5.0
pdfplumber>=0.11
fastembed>=0.4
rank-bm25>=0.2.2
numpy>=1.26
httpx>=0.27
pyyaml>=6.0

# ZeroGPU. `spaces` provides the decorator that attaches a GPU for the duration
# of a call; a Space on that hardware refuses to start without one. torch and
# transformers run the generation model there — which is why the Space needs no
# API key, unlike a hosted endpoint.
spaces>=0.30
torch>=2.4
transformers>=4.44
accelerate>=0.34
